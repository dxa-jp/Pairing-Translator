# 実機テスト手順書 — 近接音声翻訳PoC (DroidAutoConnetionSonix)

2台のAndroid実機(Google Play Services搭載)で、自動接続→音声認識→相互翻訳表示を検証する。

## 0. 事前準備(重要)

1. **サーバーでの端末承認**: アプリを一度起動すると `device_auth.php` に自己登録される。
   MultiTranslatorの管理UI(https://multitranslator.dev.x-tools.biz/admin/)で
   両端末を**承認(status=1)**しておく。未承認のままだと音声が動かない
   (画面には「未承認(管理UIで承認してください)」と出る)。
2. **両端末でインターネット接続**が必要(SonioxのSTT/翻訳はクラウド処理)。
   端末間のNearby接続はオフラインでも動くが、翻訳にはネットが要る。
3. Sonioxの**月次クォータ(USD/端末)を消費する**。常時リスニングのため
   接続中は継続的に消費される点に注意。

## 1. インストール

```bash
# USBで接続した端末ごとに
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

v0.2.5以降のアプリIDは `com.navypool.pairingtranslator`。
v0.2.4以前および旧チャットPoCとは別アプリとしてインストールされる。
設定・端末登録は自動移行されないため、初回起動時に権限・入力言語・サーバー承認を設定する。
動作確認時は旧版を終了する。

## 2. 初回起動(各端末で1回)

1. アプリを起動 → 権限ダイアログを許可(マイク・近くのデバイス。初回のみ)
2. メイン画面で**「自分の入力言語」**を選ぶ(例: 片方を🇯🇵JP、もう片方を🇺🇸US)
3. ヘッダーの「サーバー: 承認済み」を確認

## 3. 音声翻訳テスト

1. 両端末でアプリを起動し、近づけて置く(自動接続。何もタップしない)
2. 「接続済み・🎧音声認識中」になれば音声セッション稼働
3. 日本語側が話す → 日本語側に原文(大)+英訳(小)、英語側に英訳(大)+原文(小)が出る
4. 逆に英語側が話す → 同じく逆方向で流れる

**期待される動作**: 自端末のマイクに相手の声が混入しても、指定外言語と判定された結果を自分の吹き出しに混ぜない。
言語判定そのものはSonioxの精度に依存するため、2台で確認する。

## 4. 検証項目チェックリスト

| # | 項目 | 確認方法 | 結果 |
|---|------|---------|------|
| 1 | 自動接続(操作ゼロ) | 起動後に放置で「接続済み」 | |
| 2 | 音声認識+翻訳の双方向表示 | 互いの言語で話す | |
| 3 | 部分結果のライブ更新 | 話している途中で「認識中…」が更新される | |
| 4 | 相手の声が自端末で翻訳されない | 相手が話しても自端末の自分発話欄が変わらない | |
| 5 | 言語ピッカー(次回接続反映) | 言語変更→切断→再接続で新しい言語ペアになる | |
| 6 | 切断検知→マイク停止 | 相手アプリをkill→「応答がないため切断」+音声停止 | |
| 7 | 再接続→音声自動再開 | 相手アプリ再起動で自動ペア+音声再開 | |
| 8 | スリープ復帰 | 画面OFF→ONで探索/音声が自動復帰する | |
| 9 | 55分ローテーション | ログに「セッションローテーション」が出现し音声が継続 | |
| 10 | 未承認端末のエラー表示 | 管理UIで未承認の端末で話す→エラーメッセージ | |
| 11 | クォータ超過のエラー表示 | (検証可能なら)QUOTA_EXCEEDEDの表示 | |

※ ログは「▲ ログ」をタップで展開。adbなら `adb logcat -s PocChat`。

## 5. 注意・既知の制約

- Soniox WebSocketの切断(ネット不安定等)時は自動で張り直すが、
  キー取得失敗(未承認・クォータ超過・オフライン)はUIにエラーが出て停止する。
  ネット復帰後は画面の再表示(スリープOFF/ON)で再開する。
- 同じ言語同士のペア(例: 両方日本語)は想定外。
- 接続後の帯域はBLE→Wi-Fiへ自動アップグレードされる(数秒かかることがある)。
- サーバー側(MultiTranslator)には一切変更を加えていない。不具合時は
  管理UIの履歴(temp_key_history)でキー発行状況を確認できる。

## 6. サーバー指定モデルの確認（v0.2.2）

- 承認済みの2台で翻訳を開始し、Soniox接続設定のモデル名が一時キーAPIの `model` と一致することを確認する。
- テスト環境でAPIが返すモデル名を変更し、再接続後と55分ローテーション後の接続に新しいモデル名が使われることを確認する。
- テスト環境で成功応答の `model` を欠落、null、空文字、空白のみ、数値にした場合、モデル名のエラーが表示され、Soniox接続・録音を開始しないことを確認する。
- 未承認・クォータ超過の応答では、従来どおりのエラーが表示されることを確認する。

## 7. 言語混在・途中結果の確認（v0.2.4）

- 日本語端末と英語端末の両方をv0.2.4に更新する。
- 英語側で「Where are you from?」、日本語側で「日本から来ました。」と発話する。
- 日本語側の自分の吹き出しの原文が「日本から来ました。」、訳文が「I came from Japan.」となり、英語の質問が原文に混じらないことを確認する。
- 英語側でも質問の原文に日本語の返答が混じらないことを確認する。
- 途中結果は同じ吹き出しの内容が置き換わり、相手端末には発話終端で確定した結果が一度だけ追加されることを確認する。
- 切断・再接続で未完了の文字が次の吹き出しに混ざらないことを確認する。
- 自動テスト: `./gradlew :app:testDebugUnitTest`。
- 「Hi there。」「What are you doing?」に誤って日本語ラベルが付いても、自分の吹き出しへ表示・送信しないことを回帰テストで確認する。
- 「Android Studioを使っています。」のような日本語文中の英字は残ることを確認する。
- 診断ログの `Soniox確定トークン(status/lang/source)` で、翻訳対象・検出言語・翻訳元言語を確認できる。一時キーは記録しない。

### 2026-09-11 実機確認（日本語端末 CP08_J1、v0.2.4）

- ユーザーの「Hi there」「What are you doing?」が `none/en` として除外された。
- 「こんにちは。」「プログラミングをしていますよ。」のみ、自分の確定吹き出しへ追加された。
- 画面とADBログで確認。日本語端末の英語混入はこの発話で再現しなかった。
- 相手端末P30Aから届く原文・訳文のずれは残っている。P30Aはこの作業では更新しておらず、同端末のv0.2.4適用後の検証は未実施。
